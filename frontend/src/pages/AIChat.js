import React, { useState } from "react";import { GoogleGenerativeAI } from "@google/generative-ai";const genAI = new GoogleGenerativeAI(process.env.REACT_APP_GEMINI_API_KEY);const model = genAI.getGenerativeModel({ model: "gemini-1.5-flash" });export default function AIChat() {  const [input, setInput] = useState("");  const [messages, setMessages] = useState([]);  const sendMessage = async () => {    const userMsg = { role: "user", text: input };    setMessages([...messages, userMsg]);    const result = await model.generateContent(input);    const response = await result.response;    setMessages(m => [...m, { role: "ai", text: response.text() }]);    setInput("");  };  return (    <div style={{ padding: "20px", color: "white", background: "#1a1a1a", height: "100vh" }}>      <h1>SV-1500 Kingdom AI</h1>      <div style={{ border: "1px solid #333", height: "400px", overflowY: "scroll", marginBottom: "10px", padding: "10px" }}>        {messages.map((m, i) => <div key={i} style={{ color: m.role === "user" ? "#4caf50" : "#2196f3" }}><b>{m.role.toUpperCase()}:</b> {m.text}</div>)}      </div>      <input style={{ background: "#333", color: "white", width: "80%", padding: "10px" }} value={input} onChange={(e) => setInput(e.target.value)} placeholder="Analyze this deal..." />      <button onClick={sendMessage} style={{ padding: "10px", background: "#4caf50", color: "white" }}>SEND</button>    </div>  );}
